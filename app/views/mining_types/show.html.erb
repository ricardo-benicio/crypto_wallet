<p style="color: green"><%= notice %></p>

<%= render @mining_type %>

<div>
  <%= link_to t('editing.mining_type'), edit_mining_type_path(@mining_type), class: "btn btn-primary btn-lg" %> |
  <%= link_to t('links.back'), mining_types_path, class: "btn btn-primary btn-lg" %>

  <%= button_to t('links.delete'), @mining_type, method: :delete, data: 'Tem certeza?', class: "btn btn-primary btn-lg" %>
</div>
